import { createTheme } from "@mui/material";

const customeTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      // main: "#00927c",
      main: "#0097e6",
    },
    secondary: {
      main: "#EAF0F1",
    },
  },
});

export default customeTheme;
